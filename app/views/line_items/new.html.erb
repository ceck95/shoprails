<h1>New line_item</h1>

<%= render 'form' %>

<%= link_to 'Back', line_items_path %>
<%=
    link_to_unless_current(image_tag("2.png", size: "50x22"), { controller: "line_items", action: "new" }) do
       link_to("Go back", { controller: "line_items", action: "index" })
    end
 %>
<%= debug(params) %>
