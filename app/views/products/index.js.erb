// $('#remote').html('<%= escape_javascript(paginate(@products, :remote => true).to_s) %>');
// $('#remote-data').html('<%= escape_javascript(render "remote") %>');
$('#remote-data').append("<%= escape_javascript(render :partial => 'remote')%>");
$('#load_more_link').replaceWith("<%= escape_javascript(link_to_next_page(@products, 'Load More', :remote => true, :id=>'load_more_link'))%>");